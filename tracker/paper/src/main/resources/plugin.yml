name: BlockCtrlTracker
version: 2.0.0
main: dev.blockctrl.tracker.BlockCtrlTracker
api-version: '1.20'
author: BlockCtrl
description: Server-side lost-item lifecycle tracker for BlockCtrl.

commands:
  register:
    description: BlockCtrl website hesabı oluşturur
    usage: /register <password> [email]
    permission: blockctrl.register
permissions:
  blockctrl.register:
    default: true